buildscript {
    ext {
        repos = {
            gradlePluginPortal()
            mavenLocal()
            mavenCentral()
        }
        lombokVersion = '1.18.12'
        springJmsVersion = '5.2.5.RELEASE'
        activemqSpringVersion = '5.15.12'
        springBootVersion = '2.2.6.RELEASE'
        jacksonDatabindVersion = '2.10.3'
        pitestVersion = '1.4.6'
        repositories(repos)
    }
}


plugins {
    id 'groovy'
    id 'java'
    id 'idea'
    id 'org.springframework.boot' version "$springBootVersion"
    id 'info.solidsoft.pitest' version "$pitestVersion"
}

apply from: 'gradle/test.gradle'

group 'com.trzewik'
version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories(repos)

dependencies {
    implementation "org.springframework.boot:spring-boot-starter:${springBootVersion}"
    implementation "org.springframework:spring-jms:${springJmsVersion}"
    implementation "org.apache.activemq:activemq-spring:${activemqSpringVersion}"

    implementation "com.fasterxml.jackson.core:jackson-databind:${jacksonDatabindVersion}"
    implementation "com.fasterxml.jackson.core:jackson-annotations:${jacksonDatabindVersion}"
    implementation "com.fasterxml.jackson.core:jackson-core:${jacksonDatabindVersion}"

    compileOnly "org.projectlombok:lombok:${lombokVersion}"
    annotationProcessor "org.projectlombok:lombok:${lombokVersion}"
}

bootJar {
    mainClassName = 'com.trzewik.activemq.App'
    manifest {
        attributes 'Start-Class': 'com.trzewik.activemq.App'
    }
    launchScript()
}

bootRun {
    main = 'com.trzewik.activemq.App'
}
